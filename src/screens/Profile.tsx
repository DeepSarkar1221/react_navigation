import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Profile = () => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  )
}

export default Profile

const styles = StyleSheet.create({})

//navigate("")  -->Go to screen by their name
// goBack() --> Go to prev page
//push("")  -> go to any (also same) screen by name
// replace("") -> replace the curr screen
// popToTop ()--> go back to the 1st screen
//popTo("")  -> no back button 